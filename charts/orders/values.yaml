replicaCount: 2
image:
  repository: retail/orders-api
  tag: v1.0.0
service:
  type: ClusterIP
  port: 8080

egress:
  allow:
    - name: legacy
      host: "*.legacy.example.com"
      port: 443
      protocol: HTTPS
      justification: Legacy SaaS access
      ttlDays: 30
